# quick_sort
этот код сортирует произведение Пушкина "Евгений Онегин", но может и другие файлы
использование:
```
./a.out [-f input.txt] [-o output.txt]
```
в файле main.c находится весь необходимый код, остальные файлы не участвуют, и сохранены как старые версии
в файлах _index находится реализация qsort на индексах
в main.c - "стандартная" реализация

изменяя функцию main, можно проверить обе программы для сдачи - Онегин по умолчанию и переключиться на сортировку цифр при желании

в начале есть #define DEBUG_XXX if (0)
измените на 1 если необходима конкретная отладочная информация

### все строчки до единой были написаны в трезвом здравом уме и твёрдой памяти (хотя последнее не точно), было пролито много слез за ночным дебагом, и произнесено много нецензурных выражений в сторону qsort

## прошу понять, простить и не осуждать
